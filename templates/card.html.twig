{% extends "base.html.twig" %}

{% set hide_main_nav = true %}

{% block title %}Kortspelet - Funktioner{% endblock %}

{% block body %}
    <style>
        body {
            margin-top: 0;
        }

        .card-links {
            margin-bottom: 2rem;
            padding: 1rem;
            background-color: #f8f8f8;
            border-radius: 8px;
        }

        .card-links ul {
            list-style-type: none;
            padding: 0;
        }

        .card-links li {
            margin: 0.5rem 0;
        }

        a {
            text-decoration: none;
            color: #007bff;
        }

        a:hover {
            text-decoration: underline;
        }
    </style>

    <h1>Kortspelet - Funktioner</h1>

    <p>Välkommen till kortspelet! Här kan du använda olika funktioner för att interagera med kortleken och sessionen. Nedan följer de olika tillgängliga funktionerna:</p>

    <!-- Kortfunktioner -->
    <div class="card-links">
        <h2>Funktioner för kortleken</h2>
        <ul>
            <li><a href="{{ path('card_deck') }}" target="_blank">Visa kortlek</a></li>
            <li><a href="{{ path('card_shuffle') }}" target="_blank">Blanda kortlek</a></li>
            <li><a href="{{ path('card_draw_multiple', { number: 3 }) }}" target="_blank">Dra flera kort (3)</a></li>
            <li><a href="{{ path('card_deal', { players: 2, cards: 5 }) }}" target="_blank">Dela ut kort</a></li>
        </ul>
    </div>

    <!-- Sessionhantering -->
    <div class="card-links">
        <h2>Sessionhantering</h2>
        <ul>
            <li><a href="{{ path('session_debug') }}" target="_blank">Visa session</a></li>
            <li><a href="{{ path('session_delete') }}" target="_blank">Rensa session</a></li>
        </ul>
    </div>

    <!-- Länk till API Index -->
    <div class="card-links">
        <h2>API Endpoints</h2>
        <ul>
            <li><a href="{{ path('api_index') }}" target="_blank">Visa API – JSON Endpoints</a></li>
        </ul>
    </div>

    <!-- Tillbaka till huvudmenyn -->
    <p>
        <a href="{{ path('home') }}" target="_blank">⬅️ Tillbaka till huvudmenyn</a>
    </p>

{% endblock %}
