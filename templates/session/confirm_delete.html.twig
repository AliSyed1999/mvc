{% extends 'base.html.twig' %}

{% block title %}Bekräfta session rensning{% endblock %}

{% block body %}
    <h1>Bekräfta session rensning</h1>

    <p>Vill du verkligen rensa alla sessiondata?</p>

    <form action="{{ path('session_confirm_delete') }}" method="post">
        <button type="submit" class="btn btn-danger">Rensa session</button>
    </form>
    
    <a href="{{ path('home') }}">Avbryt</a>
{% endblock %}
